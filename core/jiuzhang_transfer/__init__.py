"""九章（北京 B200）数据迁移链：杭州 OSS → 九章本地盘，单跳直连。

与泰国那条（core/ssh_transfer）的区别：
  · **单跳**：bot 直接 SSH 到九章，不经新加坡中转（境内直连，无需中转桶）
  · 目的盘是 GPFS，**不是 FUSE** —— 不需要泰国那套 `--parallel 1 --part-size 5Gi`
    的单分片降速处理，可以跑满并发
  · 只有一段，状态机相应简化

刻意复用（不复制）：`core.ssh_transfer.paths` —— 那是安全边界（防命令注入的白名单），
只能有一份实现。其余为避免动到正在跑任务的 engine_ssh，本模块自带连接层。
"""
