[pytest]
# 默认跳过集成测试（需要真实外部服务）
# 跑全量集成测试：pytest -m integration
# 跑全部（含集成）：pytest -m ""
addopts = -m "not integration"

markers =
    integration: 需要真实外部服务（Grafana / Jira / LLM / 飞书 API）

# 测试路径发现
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
